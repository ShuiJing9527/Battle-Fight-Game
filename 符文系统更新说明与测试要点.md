# 符文系统更新说明与测试要点

日期：2026-06-29

本文记录本次符文系统更新内容，以及进入 Unity 后需要重点测试的项目。

## 一、本次更新目标

本次更新围绕符文掉落、符文表现、符文文本和外置参数配置展开。

核心目标：

- 符文由精英怪和 Boss 掉落。
- 符文掉落物使用 `Assets/Prefabs/RuneDrops` 文件夹内的预制体造型。
- 符文掉落参数外置化，不需要进入 Gameplay 就可以在 Inspector 中调整。
- 符文名称和说明全部改为英文，避免中文显示乱码。
- 移除旧的方块兜底表现，符文不再显示成 Cube。

## 二、主要新增内容

### 1. 外置符文掉落配置

新增配置资产：

`Assets/Settings/RuneDropSettings.asset`

可在 Inspector 中调整：

- 普通怪符文掉落数量。
- 精英怪符文掉落数量。
- Boss 符文掉落数量。
- 普通怪、精英怪、Boss 的额外掉落概率。
- 幸运值对额外符文掉落的加成。
- 幸运值额外掉落概率上限。
- 符文掉落高度。
- 符文掉落预制体列表。
- 生命、护盾、魔力、荆棘、幸运五类符文的随机权重。

当前默认值：

- 普通怪：0 个符文。
- 精英怪：1 个符文。
- Boss：2 个符文。
- 五类符文权重相同。
- 默认掉落预制体：`Assets/Prefabs/RuneDrops/5.prefab`。

### 2. 符文掉落管理器接入配置

修改脚本：

`Assets/Zhu/符文/RuneDropManager.cs`

现在逻辑为：

- 优先读取 `RuneDropSettings.asset`。
- 根据外置配置决定掉落数量、掉落预制体和符文类型权重。
- 掉落物实例化后自动写入对应 `RuneDefinition`。
- 生成物命名为英文格式，例如 `RuneDrop_Life`、`RuneDrop_Mana`。

### 3. 怪物死亡掉落接入符文配置

修改脚本：

`Assets/Scripts/Enemies/Drops/RuntimeLootDropOnDeath.cs`

现在逻辑为：

- 怪物死亡时读取自身 `MonsterIdentity.rank`。
- 普通怪、精英怪、Boss 的符文掉落数量由 `RuneDropSettings.asset` 决定。
- 不再在脚本里写死精英怪 1 个、Boss 2 个。
- 如果场景中没有 `RuneDropManager`，编辑器运行时会尝试从 `Assets/Prefabs/RuneDrops` 文件夹找预制体生成。
- 如果仍然找不到符文预制体，则不生成符文，并打印 warning。

### 4. 移除方块符文兜底

旧逻辑在找不到符文预制体时会生成 `PrimitiveType.Cube`。

本次已经移除该逻辑。

现在不会再出现：

- 符文掉落物是方块。
- 符文对象名字带中文后显示乱码。
- 缺预制体时用 Cube 临时代替。

正确结果：

- 符文只使用 `RuneDrops` 文件夹中的预制体。
- 配置缺失时不生成符文，只提示问题。

### 5. 符文英文文本

修改脚本：

`Assets/shukaisho/RuneDefinition.cs`

以下内容已全部改为英文：

- 符文名字。
- 符文描述。
- 1 件效果。
- 2 件效果。
- 3 件效果。
- 4 件效果。
- 5 件效果。
- 套装奖励说明。

当前五类符文英文名：

- `Life Rune`
- `Shield Rune`
- `Mana Rune`
- `Thorn Rune`
- `Luck Rune`

### 6. RuneLibrary 自动同步英文默认文本

修改脚本：

`Assets/shukaisho/RuneLibrary.cs`

新增逻辑：

- 默认自动同步英文符文文本。
- 已经存在于场景中的旧中文符文数据，会在脚本重载、打开场景或运行时同步成英文。
- 如果以后需要手动自定义符文文本，可以在 `RuneLibrary` 上勾选 `Keep Custom Text`。

## 三、编辑器工具

新增编辑器菜单：

`Tools/Battle-Fight-Game/Rune/Create Or Refresh Rune Drop Settings`

用途：

- 创建或刷新 `RuneDropSettings.asset`。
- 自动扫描 `Assets/Prefabs/RuneDrops` 文件夹中的符文掉落预制体。
- 将扫描到的预制体写入外置配置。

使用场景：

- 新增了符文掉落预制体。
- 替换了符文掉落造型。
- 配置里的预制体列表丢失或为空。

## 四、游戏内预期表现

### 普通怪

- 默认不掉落符文。
- 仍然可以掉落魂，具体取决于原有魂掉落逻辑。

### 精英怪

- 死亡后默认掉落 1 个符文。
- 符文掉落物应使用 `RuneDrops` 预制体外观。
- 拾取后进入符文背包。

### Boss

- 死亡后默认掉落 2 个符文。
- 掉落物同样使用 `RuneDrops` 预制体外观。
- 可通过 `RuneDropSettings.asset` 调整数量。

### 符文显示

- UI 中符文名称应显示英文。
- 说明文本应显示英文。
- 不应再出现中文乱码。
- 场景中掉落对象名应类似 `RuneDrop_Life`，不应出现乱码对象名。

## 五、测试要点

### 1. 配置检查

进入 Unity 后检查：

`Assets/Settings/RuneDropSettings.asset`

确认：

- `Elite Rune Drops` 为 1。
- `Boss Rune Drops` 为 2。
- `Rune Drop Prefabs` 中至少有一个预制体。
- 当前应包含 `Assets/Prefabs/RuneDrops/5.prefab`。
- 五类符文权重不是全 0。

### 2. 场景引用检查

打开当前主测试场景：

`Assets/Scenes/草原.unity`

找到场景中的 `RuneDropManager`。

确认：

- `Drop Settings` 已引用 `RuneDropSettings.asset`。
- `Rune Library` 存在。
- `Rune Inventory` 存在或能被拾取逻辑找到。

### 3. 精英怪掉落测试

测试步骤：

1. 进入 Play Mode。
2. 找到或生成精英怪。
3. 击杀精英怪。
4. 观察死亡位置附近是否生成符文。

通过标准：

- 精英怪死亡后生成 1 个符文。
- 掉落物不是方块。
- 掉落物使用 `RuneDrops` 预制体外观。
- Console 不应出现缺失 `RuneDropManager` 或缺失符文预制体的 warning。

### 4. Boss 掉落测试

测试步骤：

1. 进入 Play Mode。
2. 找到或生成 Boss。
3. 击杀 Boss。
4. 观察掉落数量。

通过标准：

- Boss 默认掉落 2 个符文。
- 掉落物不是方块。
- 掉落物可以被玩家拾取。

### 5. 普通怪不掉符文测试

测试步骤：

1. 进入 Play Mode。
2. 击杀普通怪。

通过标准：

- 普通怪默认不掉落符文。
- 如果普通怪掉了符文，检查 `RuneDropSettings.asset` 中 `Normal Rune Drops` 或普通怪额外概率是否被改过。

### 6. 拾取测试

测试步骤：

1. 击杀精英怪或 Boss。
2. 走到符文掉落物上。
3. 打开符文背包或技能符文界面。

通过标准：

- 符文被拾取后掉落物消失。
- 背包中出现对应符文。
- 符文名称为英文。
- 符文描述为英文。

### 7. 英文文本测试

重点检查：

- `Life Rune`
- `Shield Rune`
- `Mana Rune`
- `Thorn Rune`
- `Luck Rune`

通过标准：

- UI 中没有中文符文名。
- UI 中没有中文描述。
- 没有乱码文本。

### 8. 掉落权重测试

测试方式：

1. 在 `RuneDropSettings.asset` 中将某一类符文权重调高。
2. 将其他符文权重调低或设为 0。
3. 多次击杀精英怪。

通过标准：

- 掉落结果明显偏向权重更高的符文类型。
- 权重为 0 的符文不应被随机出来。

### 9. 新增掉落预制体测试

测试步骤：

1. 将新的符文掉落预制体放入 `Assets/Prefabs/RuneDrops`。
2. 点击菜单 `Tools/Battle-Fight-Game/Rune/Create Or Refresh Rune Drop Settings`。
3. 查看 `RuneDropSettings.asset` 的预制体列表。
4. 进入 Play Mode 击杀精英怪。

通过标准：

- 新预制体出现在配置列表中。
- 掉落时可以使用新预制体。
- 新预制体能拾取。

## 六、异常排查

### 1. 符文还是方块

可能原因：

- Play Mode 中残留了修改前已经生成的旧方块。
- 当前运行的场景不是已接入 `RuneDropSettings.asset` 的场景。
- 还有其他旧脚本在生成符文方块。

处理方式：

- 停止 Play Mode 后重新开始。
- 确认当前场景有 `RuneDropManager`。
- 搜索项目中是否还有 `PrimitiveType.Cube` 用于符文生成。

### 2. 符文没有掉落

可能原因：

- 击杀的是普通怪，普通怪默认掉落数量为 0。
- 怪物没有 `MonsterIdentity`，或者 rank 不是 Elite/Boss。
- `RuneDropSettings.asset` 中对应掉落数量被设为 0。
- `Rune Drop Prefabs` 列表为空。

处理方式：

- 检查怪物的 `MonsterIdentity.rank`。
- 检查 `RuneDropSettings.asset`。
- 点击刷新菜单重新扫描 `RuneDrops` 文件夹。

### 3. 掉落了但无法拾取

可能原因：

- 掉落预制体没有 Trigger Collider。
- 玩家对象没有被拾取逻辑识别。
- 场景中缺少共享 `RuneInventory`。

处理方式：

- 检查符文掉落预制体上的 Collider 是否为 Trigger。
- 检查玩家 Tag 或玩家组件。
- 检查 `RuneSystem` 或 `RuneDropManager` 附近是否有 `RuneInventory`。

### 4. UI 仍显示中文

可能原因：

- `RuneLibrary` 上勾选了 `Keep Custom Text`。
- 场景对象还没有触发脚本重载或 `OnValidate`。
- UI 使用了其他旧数据源。

处理方式：

- 取消勾选 `Keep Custom Text`。
- 在 `RuneLibrary` 组件菜单中执行 `Sync English Rune Text`。
- 重新打开场景或重新进入 Play Mode。

## 七、编译结果

本次更新后已执行：

`dotnet build Battle-Fight-Game.sln --no-restore`

结果：

- 0 errors。
- 存在项目原有 warning，不影响本次符文更新编译。

